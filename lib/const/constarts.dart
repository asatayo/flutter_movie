import 'package:flutter/material.dart';

//application constant padding size

const verticalHeight = SizedBox(height: 10);

const verticalWidth = SizedBox(width: 10);

 AppBarTheme appBarTheme = const AppBarTheme(
    backgroundColor: primaryColor,
    titleTextStyle:
        TextStyle(color: Colors.white, fontWeight: FontWeight.bold));

//application colors
const primaryColor = Color(0xff270027);
const primaryBackground = Colors.white;

//Application's text styles

const TextStyle styleMovieTitle =
    TextStyle(fontSize: 15, color: Colors.black87, fontWeight: FontWeight.bold);

const TextStyle styleDescriptionWhite =
    TextStyle(fontSize: 16, color: Colors.white, fontWeight: FontWeight.w400);

